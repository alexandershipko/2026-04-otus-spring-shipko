spring:
  shell:
    interactive:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: none

  datasource:
    url: jdbc:h2:mem:maindb

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml