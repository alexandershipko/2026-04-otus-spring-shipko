spring:
  jpa:
    open-in-view: false
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true

  datasource:
    url: jdbc:h2:mem:maindb

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml