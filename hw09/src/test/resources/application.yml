spring:
  shell:
    interactive:
      enabled: false

  output:
    ansi:
      enabled: always

  jpa:
    hibernate:
      ddl-auto: none

  datasource:
    url: jdbc:h2:mem:testdb

  liquibase:
    change-log: classpath:db/changelog/db.changelog-test.yaml